# Dockerfile for Harbor's nginx gateway built on a DHI nginx base.
ARG RUNTIME_IMAGE=dhi.io/nginx:1.31.2-alpine3.24

FROM ${RUNTIME_IMAGE}

USER 0
RUN mkdir -p /var/lib/nginx/logs /var/cache/nginx /var/log/nginx /run \
    && chown -R 10000:10000 /var/lib/nginx /var/cache/nginx /var/log/nginx /run

VOLUME ["/var/cache/nginx", "/var/log/nginx", "/run"]
STOPSIGNAL SIGQUIT

HEALTHCHECK CMD wget -q -O /dev/null http://localhost:8080 || exit 1

USER nginx
CMD ["-g", "daemon off;"]
